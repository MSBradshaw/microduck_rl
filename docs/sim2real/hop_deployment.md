# Deploying the hop policy on hardware

Interface spec for running a trained hop policy on the real MicroDuck.
Verified by exporting a trained checkpoint 2026-09-03.

## Export

```bash
uv run python scripts/export.py Mjlab-Hop-Flat-Sprung-K3344-MicroDuck \
    --wandb-run-path <entity/project/run_id> --onnx-file hop.onnx
```

Produces **61 float32 in -> 14 float32 out**. (Note 61, not the 51 quoted in
CLAUDE.md — the observation has grown.)

## Observation layout — 61 floats, this exact order

| idx | term | n | notes |
|---|---|---|---|
| 0:3 | `base_ang_vel` | 3 | IMU gyro, body frame |
| 3:6 | `projected_gravity` | 3 | gravity in body frame, from the IMU |
| 6:20 | `joint_pos` | 14 | **relative to HOME_FRAME**, not absolute |
| 20:34 | `joint_vel` | 14 | **one control step of lag** — see below |
| 34:48 | `actions` | 14 | the previous action, verbatim |
| 48:51 | `command` | 3 | **`[cos(2*pi*phi), sin(2*pi*phi), 0]`** — drive externally |
| 51:55 | `head_command` | 4 | runtime head pose offset; zeros = neutral |
| 55:61 | `body_command` | 6 | runtime body pose offset; zeros = neutral |

**The hop phase is not generated by the policy.** Advance `phi` externally at
`1/HOP_PERIOD` = **1.0 Hz** and feed `[cos, sin, 0]`. This mirrors what
`scripts/infer_policy.py` already does for the ground-pick task, which drives
its phase over ~4 s.

**`joint_vel` carries a deliberate 1-step lag in simulation**, matching the
Dynamixel firmware's moving-average `present_velocity`. Reading the register
directly on hardware reproduces this; do not "fix" it with a fresher estimate.

**`head_command` and `body_command` are runtime pose offsets.** They exist so
head and body pose can be commanded at deployment without retraining. Zeros give
the neutral pose; the policy was trained with them randomised, so it tolerates
non-zero values.

## Action

14 floats, **position offsets from `HOME_FRAME` in radians**, `scale = 1.0`,
`use_default_offset = True`. So the servo goal is:

```
goal_joint_angle = HOME_FRAME[joint] + action[i]
```

`HOME_FRAME` (radians) — keep in sync with `microduck_constants.py`:

```
hip_yaw          0.0        left_knee    -0.0049    neck_pitch   0.3491
left_hip_roll   -0.0873     right_knee    0.0049    head_pitch   0.3491
right_hip_roll   0.0873     left_ankle    0.453     head_yaw     0.0
left_hip_pitch  -0.4579     right_ankle  -0.453     head_roll    0.0
right_hip_pitch  0.4579
```

## Rates and servo settings

| | value |
|---|---|
| control period | **20 ms (50 Hz)** |
| `kp_fw` (Position P Gain, addr 84) | **400** |
| Position I / D gain, feedforward | **0** |
| operating mode | 3 (position) |
| supply | 8.4 V battery (trained with `vin_range` 6.5–8.2 V) |

`kp_fw = 400` is not arbitrary: a bench sweep found achieved amplitude peaks at
kp 400–800 and **falls** at 1600 while current rises 71%. Do not raise it.

## Safety — read before the first run

**The firmware Current Limit does NOT apply in Position Control Mode**
(Robotis e-manual, confirmed empirically at limits of 30/60/120 with no effect).
There is no torque ceiling protecting the gearboxes. One XL330 gearbox has
already been destroyed on this project by repeated high-torque cycling.

- The sprung policy commands **3.4x the action rate** of the rigid one
  (`action_rate_l2` −1.50 vs −0.44). High action rate means high current.
- It also **falls 7.6x more often** (`fell_over` 0.68 vs 0.089). In simulation
  that is a reset; on hardware it is an impact.
- **Tether or foam for the first attempt.** Monitor Present Temperature
  (addr 146) on the leg servos; the limit is 70 °C.

## Expected behaviour

The trained gait is a **skip**, not a symmetric two-foot hop — nothing in the
reward distinguishes them, and the policy found skipping cheaper. Both feet do
leave the ground simultaneously.

Simulated rise was 27.2 mm of CoM height for K3900 in the corrected sweep;
K3344 is untrained as of this writing, and its slightly softer spring should
land in the same band. Treat any of it as optimistic:
the actuator model still over-predicts current 3–4x in the unsaturated regime
(see `xl330_bench_2026-09-03.md`), so real hop height will likely be lower.
